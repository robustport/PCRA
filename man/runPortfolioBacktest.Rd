% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runPortfolioBacktest.R
\name{runPortfolioBacktest}
\alias{runPortfolioBacktest}
\title{Run Portfolio Backtest and Plot}
\usage{
runPortfolioBacktest(
  return_portfolio,
  portfolio_list,
  portfolio_names,
  market_return = NULL,
  rebalance_on = NULL,
  rolling_window = NULL,
  optimize_method = "CVXR",
  moment_list = NULL,
  ...,
  save_plot = TRUE,
  plot_path = "./",
  plot_name = "backtest",
  plot_main = NULL,
  plotType = "both",
  colorSet = NULL,
  ltySet = NULL,
  lwdSet = NULL
)
}
\arguments{
\item{return_portfolio}{An \code{xts} matrix of asset returns. Column names must
match the assets in each \code{portfolio.spec} object.}

\item{portfolio_list}{A list of \code{portfolio.spec} objects built
with \code{PortfolioAnalytics}.}

\item{portfolio_names}{Character vector of names corresponding to each
portfolio in \code{portfolio_list}.}

\item{market_return}{An \code{xts} single-column object of benchmark returns.}

\item{rebalance_on}{Character string passed to \code{optimize.portfolio.rebalancing()}. 
See \code{\link[xts]{endpoints}} for valid names.}

\item{rolling_window}{Positive integer. Length of the rolling estimation
window in periods.}

\item{optimize_method}{Character string specifying the solver. Default \code{"CVXR"}.}

\item{moment_list}{If different moment functions are passed into multiple GMV
portfolios, please define each moment function via this parameter. For the
portfolio that do not require moment function, please pass NULL. Example:
\code{list('custom.covRob.Rocke', NULL, NULL)}.}

\item{save_plot}{Logical. Whether to save the plot to a PNG file. Default \code{TRUE}.}

\item{plot_path}{Character string. Full file path for the PNG output.
Required when \code{save_plot = TRUE}.}

\item{plot_name}{Plot name for the PNG output. Default \code{"backtest"}.}

\item{plot_main}{Plot title for the PNG output.}

\item{plotType}{"cumRet", "drawdown", or the default is "both"}

\item{colorSet}{Optional character vector of colors passed to \code{backtest.plot()}.}

\item{ltySet}{Optional integer vector of line types passed to \code{backtest.plot()}.}

\item{lwdSet}{Optional integer vector of line width passed to \code{backtest.plot()}.}
}
\value{
A list:
\describe{
  \item{\code{returns}}{An \code{xts} matrix of period returns with one
    column per portfolio plus a \code{"Market"} column.}
  \item{\code{cumRet}}{An \code{xts} matrix of cumulative returns.}
  \item{\code{plot}}{The plot object returned by \code{backtest.plot()}.}
}
}
\description{
Perform a backtest for a list of portfolio specifications. Portfolio objectives,
constraints, rolling window, and rebalancing frequency can be customized using
the same conventions as used in \code{PortfolioAnalytics}.
}
\examples{
args(runPortfolioBacktest)
}
